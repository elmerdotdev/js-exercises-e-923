/**
 * Jack comes into a fork in the road. Create an if-else statement with the following conditions:
 * 
 * If hungry, print the message `Goes left` to the console
 * If not hungry, print the message `Goes right` to the console
 * Otherwise, print `Did not decide` to the console
 */

let hungry = true
